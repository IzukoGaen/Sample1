# Streamlit Community Cloud: pip install -r requirements.txt (often via ``uv``).
# setuptools + wheel help ``-e .`` build the ``sanitycheck`` package from pyproject.toml.
# ``streamlit_app.py`` also adds ``src/`` to sys.path if the editable install is skipped.
setuptools>=68
wheel
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
textdistance>=4.5
psutil>=5.9
streamlit>=1.28
-e .
